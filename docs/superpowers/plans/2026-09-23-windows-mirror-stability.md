# Windows 镜像稳定性实施计划

- [x] 调整 `windows/runner/scrcpy_video_plugin.cpp`
  - 为会话增加停止状态和线程安全访问。
  - 停止后禁止输入和帧通知。
  - 使用异步纹理注销回调延迟销毁会话。
  - 处理纹理注册失败及重复释放。
- [x] 调整 `lib/features/home_screen/components/home_lake_background.dart`
  - Windows 首页视频使用软件解码。
  - 检测到已知虚拟显示驱动时不创建首页视频纹理。
  - 其他平台保持硬件加速。
- [x] 增加虚拟显示驱动兼容镜像。
  - 识别 Oray、向日葵、SpaceDesk、Parsec 等驱动。
  - 高风险机器使用随包提供的官方 scrcpy 独立窗口。
  - 普通机器继续使用内嵌镜像。
- [x] 修复自动覆盖更新。
  - 禁用下载完成后重复打开交互式安装器。
  - 使用一次性 PowerShell 脚本等待旧进程退出。
  - 使用命名参数和 Inno 静默覆盖参数启动安装器。
- [x] 通过现有镜像生命周期、界面及全量回归测试覆盖改动。
- [x] 运行格式化、静态分析和相关测试。
- [x] 构建 Windows 正式版本，检查原生编译和打包资源。
- [x] 验证随包 `scrcpy 4.0`、SDL 和服务器组件可执行。
- [ ] 由出现过闪退的虚拟显示驱动电脑完成最终真机验证。

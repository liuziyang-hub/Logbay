# Logbay debug productivity (phase C)

**Date:** 2026-09-03  
**Status:** Approved / implementing  
**Out of scope:** iOS screen mirroring

## Delivered

1. Crash/ANR/Native Issues tray + jump
2. Android screenshot/record without mirror (home + mirror strip)
3. iOS subsystem/category columns on unified logs
4. In-app update: tap → download → Windows silent overwrite (`/VERYSILENT`)

## Release requirement

Publish GitHub Release on `liuziyang-hub/Logbay` with asset:
`Logbay-{version}-windows-setup.exe` (tag `v{version}`).
